{
  "name": "eventrix-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install jest --save-dev",
    "test:backend": "jest --coverage",
    "test:frontend": "cd frontend && npm run test:coverage",
    "test:all": "npm run test:backend && npm run test:frontend"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
